[package]
name = "vale-village"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
ron = "0.8"
bevy = { version = "0.15", default-features = false }

[dev-dependencies]
tempfile = "3"
